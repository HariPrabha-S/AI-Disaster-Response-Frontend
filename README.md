# Disaster Response Frontend

Basic scaffold for the Disaster Response System frontend (React + Vite).

## Getting started

```
npm install
npm run dev
```

This is a bare-bones scaffold with placeholder components/pages/modules
matching the intended project structure. Wire up real logic, styling,
and API endpoints as needed.
